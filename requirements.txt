# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Data / storage ---
asyncpg==0.30.0
pgvector==0.3.6

# --- Generation (Anthropic API, direct) ---
anthropic==1.5.0

# --- Embeddings (local, sentence-transformers; pulls in torch) ---
sentence-transformers==6.0.1

# --- Ingestion helpers ---
tiktoken==0.8.0

# --- Agentic orchestration ---
# LangGraph provides the state-machine loop. We do not use its LLM wrappers; the
# Anthropic calls are our own (app/rag/llm.py), so the model stays a config
# switch. langchain-core is a transitive dependency.
langgraph==0.3.34
langchain-core==0.3.86
